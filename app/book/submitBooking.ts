import { BookingForm } from "./page";

export const submitBooking = async (data: BookingForm) => {
  // TODO: Replace with real Firestore write via lib/firebase.ts
  // e.g. await addDoc(collection(db, "bookings"), { ...data, createdAt: serverTimestamp() });
};
